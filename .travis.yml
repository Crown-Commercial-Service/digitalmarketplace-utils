language: python
python:
  - "2.7"
  - "3.4"
install:
  - pip install --process-dependency-links -e . .[test]
script:
  - ./scripts/run_tests.sh --cov=dmutils --cov-report=term-missing
after_success:
  - coveralls
notifications:
  email: false
